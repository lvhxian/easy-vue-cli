root = true

[*]

charset = utf-8 # 编码格式
end_of_line = lf
indent_size = 2 # tabel缩进数量
indent_style = space # 缩进类型
insert_final_newline = true # 保存文件自动加上空行
trim_trailing_whitespace = true # 自动消灭结尾的多余空格
